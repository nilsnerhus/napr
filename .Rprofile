source("renv/activate.R")
if (interactive()) {
  suppressMessages(require(devtools))
}
options(
  usethis.description = list(
    "Authors@R" = utils::person(
      "Nils", "Rørstad",
      email = "202302046@post.au.dk",
      role = c("aut", "cre"),
    ),
    License = "MIT + file LICENSE"
  )
)
